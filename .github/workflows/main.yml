name: Executar Script Python

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    env:
      GOOGLE_CREDENTIALS_PATH: GOOGLE_CREDENTIALS/service_account.json

    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Restaurar service_account.json a partir do segredo
        run: |
          mkdir -p GOOGLE_CREDENTIALS
          echo "${{ secrets.GOOGLE_CREDENTIALS }}" | base64 --decode > $GOOGLE_CREDENTIALS_PATH

      - name: Instalar Dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Executar o script
        run: python Robô.murilo.py
